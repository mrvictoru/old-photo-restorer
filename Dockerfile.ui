FROM python:3.11-slim

WORKDIR /app

COPY requirements-ui.txt .
RUN pip install --upgrade pip && pip install -r requirements-ui.txt

COPY . .

EXPOSE 8964

CMD ["uvicorn", "app.server:app", "--host", "0.0.0.0", "--port", "8964"]
